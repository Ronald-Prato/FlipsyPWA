import React, { useState } from 'react'
import { TagsWrapper, InputTitle, SectionTagsContainer } from './styles.tags'

export const Tags = () => {

    return (
        <TagsWrapper>
            <SectionTagsContainer>
                <InputTitle> Tags </InputTitle>
            </SectionTagsContainer>
        </TagsWrapper>
    )
}